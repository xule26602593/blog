spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/blog_db?useUnicode=true&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root123456
  data:
    redis:
      host: redis
      port: 6379
      password:
      database: 0

# File Upload (prod)
file:
  upload-path: /app/uploads/

logging:
  level:
    com.blog: info
